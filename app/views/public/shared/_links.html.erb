<%- if controller_name != 'sessions' %>
  <div class="row" style="margin-left: 110px;">
    <h5>既に登録済みの方</h5>
  </div>
  <div class="row" style="margin-left: 150px;">
    <%= link_to "こちら", new_session_path(resource_name), style: "text-decoration:underline" %>
    <span>からログインしてください。</span>
  </div>
  
<% end %>

<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  <h5>会員登録がお済みでない方</h5><br>
  <%= link_to "こちら", new_registration_path(resource_name), style: "text-decoration:underline" %>
  <span>から新規登録してください。</span>
<% end %>
