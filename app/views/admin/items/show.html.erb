<div class="container">
 <div class="row my-5">
  <div class="col-sm-5 offset-1">
    <h4><b>商品詳細</b></h4>
   <%= image_tag @item.get_item_image(290,180),style:"margin-left:70px;"%>
  </div>
  <div class="col-sm-6">
   <table class="table table-borderless">
    <tbody>
     <tr>
       <td>商品名</td>
       <td><%= @item.name %></td>
     </tr>
     <tr>
       <td>商品説明</td>
       <td><%= @item.introduction %></td>
     </tr>
     <tr>
       <td>ジャンル</td>
       <td><%= @item.genre.name %></td>
     </tr>
     <tr>
       <td>税込価格(税抜価格)</td>
       <td><%= @item.with_tax_price.to_s(:delimited) %>(<%=@item.price%>)円</td>
     </tr>
     <tr>
       <td>販売ステータス</td>
       <td><% if @item.is_active == true %>
             販売中
             <% else %>
             販売停止中
           <% end %></td>
     </tr>
    </tbody>
   </table>
  </div>
 </div>
 <div class="row mb-3">
  <div class="col-sm-12">
    <div class="text-center">
     <%= link_to "編集する",edit_admin_item_path(@item.id),class: 'btn  btn01 col-sm-2' %>
   </div>
  </div>
 </div>
</div>